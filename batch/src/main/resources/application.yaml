server:
  port: 8003
  servlet:
    context-path: /batch
spring:
  application:
    name: batch
  datasource:
    url: jdbc:mysql://192.168.31.121:3306/train_batch?characterEncoding=UTF8&autoReconnect=true&serverTimezone=Asia/Shanghai
    username: train_batch
    password: Batch123
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis:
  mapper-locations: classpath:/mapper/**/*.xml
logging:
  level:
    com.wj.train.batch.mapper: trace